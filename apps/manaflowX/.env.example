# This environment variable is used in Production and Preview environments to deploy your Convex project
# This variable is not needed in development.
# You can create a Production or Preview deploy key at https://dashboard.convex.dev/project/settings#production-deploy-keys
CONVEX_DEPLOY_KEY=

# X (Twitter) OAuth 2.0 credentials
# Get these from https://developer.x.com/en/portal/dashboard
# 1. Create a project and app
# 2. Enable OAuth 2.0 under "User authentication settings"
# 3. Set the callback URL to: https://your-convex-site.convex.site/twitter_callback
# 4. Copy the Client ID and Client Secret
X_API_KEY=your_client_id
X_API_KEY_SECRET=your_client_secret

# Public environment variables (accessible in browser)
NEXT_PUBLIC_X_CLIENT_ID=your_client_id
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-convex-site.convex.site
NEXT_PUBLIC_APP_URL=http://localhost:3000
