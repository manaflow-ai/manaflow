{
  "name": "@cmux/screenshot-collector-host",
  "version": "0.0.1",
  "description": "Standalone screenshot collector that runs via Claude Agent SDK",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build ./src/index.ts --outfile dist/index.js --target bun --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.6",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/bun": "^1.2.15",
    "typescript": "^5.8.3"
  }
}
